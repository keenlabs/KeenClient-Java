android {
    publishing {
        singleVariant('java') {
            withSourcesJar()
            withJavadocJar()
        }
    }
}

apply from: file("${parent.rootDir}/gradle/gradle-mvn-publish.gradle")
